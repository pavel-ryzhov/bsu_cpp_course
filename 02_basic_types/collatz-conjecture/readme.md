# Гипотеза Коллатца

Рассмотрим следующее преобразование:

$$

f(n) = \begin{cases}
  n / 2   & \mathrm{if}\; n \% 2 == 0\\
  3n + 1  & \mathrm{if}\; n \% 2 == 1\\
\end{cases}
$$

Возьмём натуральное число $n$ и построим последовательность, используя это преобразование:
$a_0 = n$, $a_i=f(a_{i−1}), \; i \geqslant 1$. Эта последовательность может либо рано или поздно прийти в единицу, либо нет (например, зациклиться или постоянно генерировать новые числа).
Пример для $n = 11$:

$$
11 \rightarrow 34 \rightarrow 17 \rightarrow 52 \rightarrow 26 \rightarrow 13 \rightarrow 40 \rightarrow 20 \rightarrow 10 \rightarrow 5 \rightarrow 16 \rightarrow 8 \rightarrow 4 \rightarrow 2 \rightarrow 1
$$

Ваша задача — для заданного числа $n$ проверить, обращается ли в единицу последовательность, начинающаяся с числа $n$. Если обращается, то функция `IterationsToConverge` должна вернуть необходимое количество итераций, а иначе — `std::nullopt`.
Обратите внимание, что если последовательность не сходится к 1, то программа не должна зациклиться. Можно считать, что достаточно проверить 100 000 первых шагов.


### Примечания
* https://en.wikipedia.org/wiki/Collatz_conjecture
* https://en.cppreference.com/w/cpp/utility/optional
