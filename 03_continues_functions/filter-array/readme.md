# Удаление нулей из массива

Дан массив из `n` чисел. Требуется «Уплотнить» его, удалив нулевые элементы.
Попробуйте сделать это без дополнительной памяти (без вспомогательных массивов и других контейнеров, но разрешено использовать локальные переменные).
